import React,{useEffect,useMemo,useState}from'react';import{Routes,Route,Link,NavLink,useNavigate,useLocation}from'react-router-dom';import{Menu,X,Home,Sparkles,Shield,Database,Download,Wrench,Search,ArrowUpRight,KeyRound,LogIn,UserPlus,LogOut,User,RefreshCw,Bot,Copy,Check,Terminal}from'lucide-react';import{Turnstile}from'@marsidev/react-turnstile';import{supabase}from'./lib/supabase';
const API=import.meta.env.VITE_API_BASE_URL||'/api',TS=import.meta.env.VITE_TURNSTILE_SITE_KEY||'';
const cats=[['ai','AI',Sparkles],['admin','ADMIN',Shield],['cache','CACHE',Database],['download','DOWNLOAD',Download],['tools','TOOLS',Wrench]];
const eps=[['ai','GET','AI Aiko','/api/ai/aiko'],['ai','GET','AI Lyrics Generator','/api/ai/lyricsgen'],['ai','GET','AI Coder','/api/tools/aicoder'],['ai','GET','Text To Image','/api/ai/text2img'],['admin','GET','Health','/api/health'],['admin','GET','Profile','/api/me'],['cache','GET','Cache Status','/api/cache/status'],['cache','GET','Cache Stats','/api/cache/stats'],['cache','GET','Cache Clear','/api/cache/clear'],['download','GET','TikTok','/api/download/tiktok'],['download','GET','Instagram','/api/download/instagram'],['download','GET','YouTube','/api/download/youtube'],['download','GET','Pinterest','/api/download/pinterest'],['download','GET','Spotify','/api/download/spotify'],['tools','GET','QRIS Generator','/api/tools/qrisgen'],['tools','GET','Short URL','/api/tools/shorturl'],['tools','GET','Screenshot','/api/tools/screenshot'],['tools','GET','IP Info','/api/tools/ipinfo']].map(x=>({cat:x[0],method:x[1],name:x[2],path:x[3]}));
function Nav({to,icon:Icon,label,n,close}){return <NavLink to={to} onClick={close} className={({isActive})=>'nav '+(isActive?'active':'')}><Icon size={21}/><span>{label}</span>{n&&<small>{n}</small>}</NavLink>}
function Shell({children}){const[open,setOpen]=useState(false);const[user,setUser]=useState(null);useEffect(()=>{supabase.auth.getSession().then(({data})=>setUser(data.session?.user||null));const{data:l}=supabase.auth.onAuthStateChange((_e,s)=>setUser(s?.user||null));return()=>l.subscription.unsubscribe()},[]);return <><header className="top"><button className="hamb" onClick={()=>setOpen(1)}><Menu/></button><Link className="brand" to="/"><i>D</i> DINSTORE <b>API</b></Link><div>{user?<Link className="user" to="/dashboard"><User size={16}/> MEMBER</Link>:<Link className="user" to="/login"><LogIn size={16}/> LOGIN</Link>}</div></header><div className="layout"><aside className={'side '+(open?'show':'')}><div className="sidehead"><div><small>NAVIGATION</small><strong>DINSTORE API</strong></div><button className="hamb close" onClick={()=>setOpen(0)}><X/></button></div><nav><Nav to="/" icon={Home} label="HOME" close={()=>setOpen(0)}/>{cats.map((c,i)=><Nav key={c[0]} to={'/docs/'+c[0]} icon={c[2]} label={c[1]} n={'0'+(i+1)} close={()=>setOpen(0)}/>) }<Nav to="/dashboard" icon={User} label="MEMBER" close={()=>setOpen(0)}/></nav><div className="sidebottom">{user?<button onClick={()=>supabase.auth.signOut()}><LogOut size={16}/> LOGOUT</button>:<Link to="/register"><UserPlus size={16}/> DAFTAR</Link>}</div></aside>{open&&<div className="overlay" onClick={()=>setOpen(0)}/>}<main>{children}</main></div><a className="float" href="https://wa.me/6287776581216" target="_blank" rel="noreferrer"><Bot/><em/></a></>}
function Card({e}){return <article className="card"><div className="cardtop"><span className="method">{e.method}</span><div><h3>{e.name}</h3><code>{e.path}</code></div><Link className="open" to={'/endpoint?path='+encodeURIComponent(e.path)}>OPEN <ArrowUpRight size={15}/></Link></div><div className="desc">API endpoint DINSTORE.</div></article>}
function Home(){const[q,setQ]=useState(''),[f,setF]=useState('all');const list=useMemo(()=>eps.filter(e=>(f==='all'||e.cat===f)&&(`${e.name} ${e.path}`).toLowerCase().includes(q.toLowerCase())),[q,f]);return <div className="page"><section className="hero"><div className="terminal">● TERMINAL ACTIVE</div><h1>DINSTORE <span>API</span></h1><p>A comprehensive and user friendly API solution for modern applications.</p></section><section className="stats"><div><small>CATEGORIES</small><b>5</b></div><div><small>ENDPOINTS</small><b className="green">18+</b></div><div className="full"><small>STATUS</small><b>ONLINE</b></div></section><div className="notice"><KeyRound/><span>Login untuk mendapatkan <b>API KEY</b> member dan mengakses endpoint.</span><Link to="/login">LOGIN →</Link></div><div className="search"><Search/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="SEARCH ENDPOINT / CATEGORY..."/></div><div className="filters"><button className={f==='all'?'sel':''} onClick={()=>setF('all')}>ALL ({eps.length})</button>{cats.map(c=><button key={c[0]} className={f===c[0]?'sel':''} onClick={()=>setF(c[0])}>{c[1]}</button>)}</div><div className="grid">{list.map(e=><Card key={e.path} e={e}/>)}</div></div>}
function Docs({id}){const c=cats.find(x=>x[0]===id)||cats[0],list=eps.filter(e=>e.cat===id);return <div className="page"><div className="heading"><div><small>MODULE</small><h1>{c[1]}</h1><p>{list.length} endpoints</p></div></div><div className="grid">{list.map(e=><Card key={e.path} e={e}/>)}</div></div>}
function Auth({title,sub,children}){return <div className="auth"><div className="authcard"><div className="brand big"><i>D</i> DINSTORE API</div><small>MEMBER ACCESS</small><h1>{title}</h1><p>{sub}</p>{children}</div></div>}
function Login(){const nav=useNavigate(),[email,setEmail]=useState(''),[pw,setPw]=useState(''),[err,setErr]=useState('');return <Auth title="LOGIN MEMBER" sub="Masuk ke dashboard DINSTORE API."><form onSubmit={async e=>{e.preventDefault();setErr('');const{error}=await supabase.auth.signInWithPassword({email,password:pw});if(error)setErr(error.message);else nav('/dashboard')}}><label>Email<input type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Password<input type="password" required value={pw} onChange={e=>setPw(e.target.value)}/></label>{err&&<div className="error">{err}</div>}<button className="primary">LOGIN</button><div className="links"><Link to="/forgot-password">Lupa sandi?</Link><Link to="/register">Daftar</Link></div></form></Auth>}
function Register(){const nav=useNavigate(),[v,setV]=useState({name:'',email:'',pw:''}),[token,setToken]=useState(''),[err,setErr]=useState(''),[ok,setOk]=useState('');return <Auth title="DAFTAR MEMBER" sub="Buat akun baru dan dapatkan API key."><form onSubmit={async e=>{e.preventDefault();setErr('');if(TS&&!token)return setErr('Selesaikan verifikasi Cloudflare Turnstile.');const{data,error}=await supabase.auth.signUp({email:v.email,password:v.pw,options:{data:{name:v.name}}});if(error)return setErr(error.message);if(data.session)nav('/dashboard');else setOk('Akun dibuat. Cek email verifikasi lalu login.')}}><label>Nama<input required value={v.name} onChange={e=>setV({...v,name:e.target.value})}/></label><label>Email<input type="email" required value={v.email} onChange={e=>setV({...v,email:e.target.value})}/></label><label>Password<input type="password" minLength="8" required value={v.pw} onChange={e=>setV({...v,pw:e.target.value})}/></label>{TS&&<Turnstile siteKey={TS} onSuccess={setToken}/>} {err&&<div className="error">{err}</div>}{ok&&<div className="ok">{ok}</div>}<button className="primary">CREATE ACCOUNT</button><div className="links"><Link to="/login">Sudah punya akun?</Link></div></form></Auth>}
function Forgot(){const[email,setEmail]=useState(''),[msg,setMsg]=useState(''),[err,setErr]=useState('');return <Auth title="LUPA PASSWORD" sub="Link reset akan dikirim ke email."><form onSubmit={async e=>{e.preventDefault();const{error}=await supabase.auth.resetPasswordForEmail(email,{redirectTo:location.origin+'/reset-password'});if(error)setErr(error.message);else setMsg('Link reset password terkirim.')}}><label>Email<input type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label>{err&&<div className="error">{err}</div>}{msg&&<div className="ok">{msg}</div>}<button className="primary">KIRIM LINK</button><Link className="back" to="/login">← Kembali</Link></form></Auth>}
function Reset(){const[pw,setPw]=useState(''),[msg,setMsg]=useState(''),[err,setErr]=useState('');return <Auth title="RESET PASSWORD" sub="Masukkan password baru."><form onSubmit={async e=>{e.preventDefault();const{error}=await supabase.auth.updateUser({password:pw});if(error)setErr(error.message);else setMsg('Password berhasil diubah.')}}><label>Password baru<input type="password" minLength="8" required value={pw} onChange={e=>setPw(e.target.value)}/></label>{err&&<div className="error">{err}</div>}{msg&&<div className="ok">{msg}</div>}<button className="primary">SIMPAN</button></form></Auth>}
function Dashboard(){const nav=useNavigate(),[p,setP]=useState(null),[key,setKey]=useState(''),[err,setErr]=useState('');useEffect(()=>{(async()=>{const{data}=await supabase.auth.getSession();if(!data.session)return nav('/login');const r=await fetch(API+'/me',{headers:{Authorization:'Bearer '+data.session.access_token}});const j=await r.json();setP(j.profile)})()},[]);const regen=async()=>{const{data}=await supabase.auth.getSession();const r=await fetch(API+'/keys/regenerate',{method:'POST',headers:{Authorization:'Bearer '+data.session.access_token}});const j=await r.json();if(!r.ok)setErr(j.error||'Gagal');else setKey(j.apiKey)};return <div className="page"><small>MEMBER PANEL</small><h1>Dashboard</h1><p>{p?.email}</p><div className="dash"><div><small>NAMA</small><b>{p?.name||'-'}</b></div><div><small>STATUS</small><b className="green">{p?.status||'active'}</b></div><div className="key"><small>API KEY BARU</small><code>{key||'••••••••••••••••••••'}</code><button onClick={regen}><RefreshCw size={16}/> GENERATE / REGENERATE</button></div></div>{err&&<div className="error">{err}</div>}<div className="security"><Terminal size={18}/> API key dibuat dan diverifikasi di backend. Service-role key tidak pernah dikirim ke browser.</div></div>}
function Endpoint(){const p=new URLSearchParams(useLocation().search),path=p.get('path')||'/api/health',[copied,setCopied]=useState(false);const copy=()=>{navigator.clipboard.writeText(location.origin+path);setCopied(true);setTimeout(()=>setCopied(false),1200)};return <div className="page"><small>DOCUMENTATION / ENDPOINT</small><div className="detail"><div className="detailhead"><span className="method">GET</span><div><h1>{eps.find(e=>e.path===path)?.name||'API Endpoint'}</h1><code>{path}</code></div></div><p>Endpoint DINSTORE API. Gunakan API key member bila endpoint memerlukannya.</p><div className="code"><span>{location.origin+path}</span><button onClick={copy}>{copied?<Check/>:<Copy/>}</button></div></div></div>}
export default function App(){return <Shell><Routes><Route path="/" element={<Home/>}/>{cats.map(c=><Route key={c[0]} path={'/docs/'+c[0]} element={<Docs id={c[0]}/>}/>) }<Route path="/endpoint" element={<Endpoint/>}/><Route path="/login" element={<Login/>}/><Route path="/register" element={<Register/>}/><Route path="/forgot-password" element={<Forgot/>}/><Route path="/reset-password" element={<Reset/>}/><Route path="/dashboard" element={<Dashboard/>}/><Route path="*" element={<Home/>}/></Routes></Shell>}
